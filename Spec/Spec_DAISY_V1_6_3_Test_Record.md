# DAISY v1.6.3 测试记录

- 对应计划：[v1.6.3 UI 整理计划](Spec_DAISY_V1_6_3_Release_Plan.md)
- 测试日期：2026-08-10
- 状态：本地开发检查点；未推送、未建立 tag
- 基线提交：`cfa3fa3e522916fc18c84d083d01b8b10ae67341`

## 一、当前版本与数据契约

| 项目 | 值 |
|---|---|
| DBS 应用版本 | 1.6.3 |
| STG 应用版本 | 1.6.3 |
| 旧版 DBS schema | 3 |
| 统一扫描 schema | 4 |
| STG 归档 schema | 3 |
| 最低只读封存库兼容版本 | v1.4.1/schema 3 FULL |

应用版本发生变化，数据库和归档结构版本均未变化。

## 二、已完成检查

| 检查 | 结果 |
|---|---|
| `py_compile`：GUI 与 GUI 单元测试 | 通过 |
| `git diff --check` | 通过 |
| 顶部入口顺序、短名、单排与按钮比例 | 通过 |
| 扫描／核验目录列表组件一致性及映射文本保留 | 通过 |
| 解析数据库／检测硬盘浅绿色操作样式 | 通过 |
| 3 个硬盘槽位等宽、等高、同背景和同边框 | 通过 |
| GUI 安全配置恢复与完整页面标题 | 通过 |
| 菜单信息结构、短名称与正文同字号 | 通过 |
| 全任务表单无下拉框；续传重试范围使用按钮 | 通过 |
| 数据库解析预设原位切换、对象身份与格式选择保持 | 通过 |
| 解析数据库成功／失败／等待期间不重建表单或切换面板 | 通过 |
| 数据模块靠左排列；固定尺寸与响应式换行保持 | 通过 |
| 重置软件顶部入口、菜单去重与统一任务操作行 | 通过 |
| 结果目录提示默认关闭、持久化、按钮短暂提示 | 通过 |
| 默认窗口 `1600×900`、工作区约束与三档窗口菜单 | 通过 |
| 现行规范术语、续传边界、相对链接与历史文档定位 | 通过 |
| v1.4.1 FULL Reader／Diff／核验／解析兼容专项 | 128/128 通过 |

首轮定向 Tk 回归共 7 项，结果 7/7 通过，用时 12.324 秒。首轮曾发现测试在切页后读取已销毁控件，修正测试取值时机后重跑通过；该问题不属于 GUI 运行故障。

随后加入版本常量、STG 归档 Manifest、目录参数和真实 Tk 顶部入口稳定性检查，扩展定向验收共 13 项，结果 13/13 通过，用时 12.793 秒。

把窗口收窄到 1100 px，并让第二个硬盘槽位使用会实际换行的长说明后，顶部入口和硬盘槽位 2 项真实 Tk 复检为 2/2 通过，用时 0.655 秒。

v1.4.1 FULL 专项沿用 v1.6.2 的冻结金样与 13 个 Reader／Diff／核验／问题报告／数据库解析测试模块，结果 128/128 通过，用时 18.075 秒，退出码 0。该结果确认本轮 UI 整理与应用版本号更新没有改变旧 FULL 库的只读消费结果。

2026-08-10 的第二轮 UI 收敛先执行 16 项 GUI／配置定向用例，覆盖菜单树、字体、环境按钮、
文件选择按钮、全表单按钮选择、数据库解析预设循环、完成提示音、结果目录提示与按钮变色，
结果 16/16 通过，用时 3.053 秒。随后执行 5 项统一扫描真实 Tk 按钮回归，覆盖顶部六入口、
二元按钮、模式展开和 RAW 能力门控，结果 5/5 通过，用时 1.500 秒。两组均未启动业务扫描、
外部工具或安装流程。

本轮的「无下拉框」测试遍历全部现行及冻结兼容 `TaskSpec` 的非顶部离散字段；重复点击当前
按钮保持控件身份和文字，切换其他按钮后保存的仍是既有内部参数值。数据库解析预设按
「摘要 → 全部 → 自定义 → 摘要」切换时，`form_inner`、预设控件、模块池和输出格式控件均
保持同一对象，手工关闭的 CSV 不会因预设切换重新开启。

正式六页面按钮矩阵覆盖 2 种候选字体 × 3 档 Tk scaling × 3 档字号 × 3 种几何 × 6 个页面，
共 324 组，结果通过，用时 26.936 秒。首轮完整 GUI 类进一步发现：特大字号＋1100×850 时
环境页最右按钮越界约 27 px，且底部 `ttk.Button` 与表单 `tk.Button` 的实际高度不一致。
修复方式为缩小环境按钮横向内边距，并让底部任务操作直接复用表单按钮的几何参数；没有通过
放宽断言掩盖问题。

修复后，全 TaskSpec 的字体／字号／六种窗口几何矩阵通过，用时 47.092 秒；3 档相对缩放 ×
3 档字号 × 4 种窗口 × 全 TaskSpec 矩阵通过，用时 19.277 秒。完整
`TestGuiArguments` 结果 123/123 通过，用时 87.834 秒；完整 GUI Scan 结果 40/40 通过，
用时 40.048 秒。当前第二轮代码上的 v1.4.1 FULL 专项再次得到 128/128 通过，用时
17.391 秒，退出码 0。

将「打开结果目录」移到「停止」旁并统一任务操作按钮后，最终定向复检 8/8 通过，用时
2.025 秒。复检覆盖普通／可控／扫描任务的按钮顺序与实际几何、结果目录短暂提示及可选弹窗、
菜单字体、环境检测按钮和数据库解析格式按钮；没有启动业务扫描或外部工具。

后续小幅调整将功能菜单末组更名为「环境」，将高级菜单项改为「命令预览」，并统一五项
核验说明的「使用……检查」句式。结果目录按钮常态改用色带琥珀色，成功后执行两次完整的
绿色脉冲。顶部功能按钮进一步与环境页六列统一横向起点、宽度和间距，并分别验证普通与
紧凑布局。相关菜单、说明、按钮比例、六列实际坐标、Tk 提示态和两次状态序列定向复检
8/8 通过，用时 1.271 秒；没有启动业务扫描或外部工具。

扫描页哈希按钮的关闭态更名为「不采集」，开启态保留 `SHA-256`。按钮往返和既有参数映射
定向复检 2/2 通过，用时 0.414 秒；内部值仍为 `none`／`full`，没有修改扫描逻辑。

数据库解析和底部操作区继续做定向调整：选择数据库及输入框失焦均不自动解析；来源版本取自
数据库描述符；数据模块使用绿色已选、黄色可选、灰色不可用按钮。底部从右向左固定为
开始／停止、暂停／继续、打开结果目录，暂停后才显示保存并退出。同一开始按钮的命令、文字与
配色会随运行状态一起切换。首轮相关 Tk 与状态测试 9/9 通过，用时 1.910 秒；随后补充的动态
命令绑定测试 1/1 通过，用时 0.255 秒。合并全部改动后，连同扫描参数映射和实际版本文案在内的
最终定向复检 12/12 通过，用时 2.116 秒。测试未启动业务扫描、外部工具或数据库写入。
随后将「暂停／继续」与「打开结果目录」对调；操作顺序和实际 Tk 几何定向复检 2/2 通过，
用时 0.341 秒。

数据模块区随后改为固定尺寸按钮和四边统一留白；窗口宽度变化时只重新换行，连续相同列数的
Configure 事件不再重复重排。快照 15 个模块使用不超过六字的 GUI 专用名称，稳定模块 ID、
导出标题和数据库协议不变；全选／取消选择与浏览按钮同尺寸。覆盖 15 项名称、宽窄布局尺寸、
三色状态、导出范围原位切换、格式按钮和 v1.4.1 版本摘要的定向复检 4/4 通过，用时
1.045 秒；未读取真实数据库或启动外部工具。

本轮小幅收敛把数据模块改为靠左排列，并让「解析数据库」在成功、失败和后台等待期间原位
更新模块池、说明与进度，不再收起／展开面板、销毁 `form_inner` 或把滚动位置主动重置到
顶部。「打开结果目录」常态完整复用顶部六入口的背景、文字、悬停色和边框；当前
1840×1020 Tk 测试窗口中，日志栏与底部三个常驻任务按钮实测 X 坐标同为
`1489/1623/1757`，宽度均为 122 px，底部高度为 39 px，
与扫描模式按钮一致。「收起模块」和「收起设置」实测 X 坐标均为 1749，且所有标题区工具
按钮均为与「添加／浏览」相同的 122×35 px。顶部入口改为「重置软件」，设置菜单不再重复
提供恢复命令。最终相关单元与真实 Tk 定向复检 21/21 通过，用时 18.038 秒；测试只使用
内存探针、工作区合成夹具和测试临时目录，没有启动业务扫描、外部工具或安装流程。

默认窗口改为 `1600×900` 后，先执行默认配置、屏幕约束与真实 Tk 六页面专项，结果 3/3
通过，用时 0.644 秒。六个页面的初始状态在标准字号下不显示滚动条；完整／快速扫描选择
全新生成或使用续传后，只有真实内容溢出才显示滚动条，末项仍可到达。测试没有把
`1600×900` 冒充为 1080p：原有两条 1080p 用例改为显式固定 `1840×1020` 客户区。

续传用户动作和数据库状态的 8 项定向专项结果 8/8 通过，用时 0.325 秒，覆盖同进程暂停／
继续、保存并退出写入建议续传、停止只允许手动续传、异常进程恢复、新 session、恢复卡片
必须由用户明确准备，以及一次完成与多次 session 的业务投影等价。

最终完整 `TestGuiArguments` 结果 134/134 通过，用时 84.746 秒。首轮发现一条历史测试仍
要求「添加」小于「恢复默认」，与现行标题区小按钮统一尺寸的设计冲突；修正为等宽等高后
全类重跑通过。统一扫描 GUI 与 schema 4 状态机两文件合计 62/62 通过，用时 46.350 秒。
首轮暴露的两条 1080p 测试前提和一条哈希关闭态文字断言均属于测试过时：前者改为显式
1080p 几何，后者按现行「不采集」文字核对；重跑没有产品失败。

当前代码上的 v1.4.1 FULL 专项再次得到 128/128 通过，用时 25.039 秒，退出码 0。
GUI 参数类、统一扫描 GUI／状态机和兼容专项为三组不重叠的最终测试，共 324 项通过。
静态检查另确认 GUI 与两份测试文件可编译、`git diff --check` 通过、全部 Markdown 相对
链接目标存在、未使用 Wiki／Obsidian 双方括号链接，并且全部受 Git 管理的文本文件均为
UTF-8 无 BOM、LF。规范审查修正了过时的 1920×1080 默认值、六字入口、模块「可导出」
文字、独立停止按钮描述和 v1.6.0 数据库解析设计中的时间限定；历史计划与阶段测试记录
保留当时事实，
通过文件头阅读说明与现行技术规格区分。

## 三、发布前待完成

- 正式发布前运行仓库完整自动化套件，并补记测试数、耗时和提交哈希。
- 用户未明确要求前，不执行推送、`main` 快进或 `v1.6.3` tag 创建。
