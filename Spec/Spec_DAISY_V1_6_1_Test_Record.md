# DAISY v1.6.1 测试记录

- 对应计划：[v1.6.1 UI 与交互优化实施计划](Spec_DAISY_V1_6_1_Implementation_Plan.md)
- 最终回归日期：2026-08-08
- 平台：Windows／Python 3.14.5／真实 Tcl/Tk
- 结果：仓库自动化 633／633 通过，failure 0、error 0
- 版本定位：初步修改版、非稳定发布；只推送 `Codex` 分支，不建立 `v1.6.1` tag

## 一、安全范围

- 未读取、写入、删除、枚举或暂存用户未跟踪的 `TEMP/`；
- 未枚举、附加、复用或终止用户已有进程；
- 未运行真实物理硬盘检测，未读取工作区外真实档案；
- 测试使用仓库合成夹具、系统临时目录及测试自己创建的受控工作目录；
- Tk 测试只创建透明／隐藏的自有窗口，并在每个用例结束时取消回调、销毁解释器；
- 子进程仅由测试自身创建，结束时显式等待或回收；未调用用户正在运行的 ffmpeg、ExifTool
  或其它外部进程；
- rawpy 安装、WinGet 安装、UAC、提示音和打开目录全部使用 mock，不发生联网安装或外部写入。

## 二、最终回归

执行命令：

```powershell
python -B -W error -m unittest discover -s .\Script\Test -p "Script_DAISY_Test_*.py" -q
```

最终结果：

```text
Ran 633 tests in 191.616s
OK
```

测试中的“无法创建运行事件证据，已拒绝开始扫描：fixture”是故障注入用例的预期日志：
用例要求证据创建失败时拒绝扫描，不能把它解释为产品回归失败。

| 测试域 | 数量 | 结果 | 主要覆盖 |
|---|---:|---:|---|
| 核心单元与真实 Tk | 236 | 通过 | GUI 外壳、DDL、扫描、Diff、路径、元数据、关闭与窗口矩阵 |
| 统一扫描 GUI | 37 | 通过 | 两阶段扫描、按钮／下拉、RAW 能力、环境页、结束布局 |
| 扫描 CLI 与工具恢复 | 33 | 通过 | 新配置、旧 partial 缺键、ExifTool 恢复、ffprobe 故障分类 |
| Hash／Run／State／Publication／No-Clobber | 122 | 通过 | worker、timeout、暂停、恢复、lease、发布与冲突保护 |
| RAW／证据／扫描接线 | 28 | 通过 | 隔离解码、证据恢复、伴随发布、取消与失败清理 |
| 统一核验 | 71 | 通过 | CLI、兼容入口、五工具独立选择、RAW-only、报告与监督器 |
| Reader／跨版本 Diff／Issues | 41 | 通过 | schema 3／4 识别、只读投影、分板块人读报告 |
| 数据库解析 | 38 | 通过 | 模块计划、CSV／JSONL、HTML／XLSX、CLI、staging 与 manifest |
| STG | 27 | 通过 | 只读命令、ZIP 三类 JSON、身份、CRC 与禁止修改操作 |
| 合计 | 633 | 通过 | warning 视为失败 |

## 三、真实 Tk 与 UI 组合验收

### 3.1 页面和交互

- 顶部只公开 6 个功能入口，保持同一行、六字标题、等高等宽；选中态不改变几何尺寸；
- 统一扫描初始只显示“扫描模式”，选择 Full／Quick 后才显示“生成方式”，再展开适用设置；
- 扫描页不显示格式校验或 RAW 深检，也不生成对应扫描参数；
- 数据核验显示内置、ExifTool、ffprobe、7-Zip、RAW 五个独立同排按钮，默认全部关闭；
- ENV 六个状态按钮和五个安装按钮分别同排；固定间隔使用独立 spacer 列，不再让末项宽 8 px；
- 普通路径浏览按钮使用统一小尺寸；“添加目录”在内容区上方并与浏览按钮左边缘对齐；
- 下拉模式重选当前项后文字保持深色、选区清空、内部值不变；选择另一项后重建仍有文字；
- 标签、输入区和单行按钮纵向居中；`NTFS标识` 的 `N` 与完整标题宽度均未裁切；
- 悬停字段文字、字段单元格或具体工具按钮均可显示相应完整说明，不再只依赖提示符号。

### 3.2 字体、缩放和窗口

真实窗口矩阵覆盖：

- 字体：`Microsoft YaHei UI`、`Noto Sans SC` 等本机候选字体；
- 字号：标准、较大、特大 3 档；
- Tk scaling：基准的 1.00、1.25、1.50 倍；
- 几何：1840×1020、1440×900、1280×720、1280×960、1280×1024、1100×850；
- 设置菜单目标：1920×1080、1600×900、1366×768；
- 二元 UI：按钮模式与下拉菜单模式。

验收结果：

- 默认 1080P 下 6 个正式页面均不需要纵向滚动；
- 内容在 2 px Tk 几何容差内时隐藏滚动条、滚轮保持顶部，不产生肉眼可见的空白滚动；
- 内容真正溢出时滚动条出现，滚轮可从顶部到达最后一项并返回顶部；
- 1100 px 和特大字号下，状态、安装和核验按钮仍在同一行，实际宽度不小于文字请求宽度；
- 五个核验按钮的像素取整差不超过 1 px；没有遮挡、越界或末项额外变宽；
- 顶部六入口要求保持单排，因此 1024 px 不列为正常支持宽度；窗口以实际工具栏最小宽度
  约束，而不是裁掉按钮来伪装支持。

### 3.3 运行布局

- 点击开始任务后收起设置、展开进度和日志，日志填充默认窗口剩余高度；
- 整批队列结束后重新展开设置；处于小窗时先返回完整界面；
- 安装任务完成后等待自动环境复检结束再展开，避免中途闪动；
- 硬盘检测开始时显示进度和“正在检测”；成功后弹窗并返回展开的硬盘选择区；
- 硬盘检测失败时也重新展开设置，同时保留进度和日志供诊断；
- 独立日志窗口为单例，与主日志双向同步清空和追加，关闭后不残留 Tcl 回调。

## 四、数据库与 v1.4.1 兼容

- `Script_DAISY_Test_DBS_Reader` 22／22 通过，覆盖冻结的 v1.4.1／schema 3 合同哈希、
  snapshot／Diff 身份、Quick／No-Hash 能力降级、Raw Payload 与输入字节不变；
- schema 3→3、3→4、4→3、4→4 Diff 回归通过，方向语义和旧库能力不足保持明确；
- v1.4.1 Full 库不需要历史格式校验字段即可进入统一核验和数据库解析；缺失能力显示
  `NULL／unavailable`，不回写、不迁移、不伪造 0；
- Issues 的枚举、哈希、Exif／元数据、格式、RAW、性能候选和运行证据板块回归通过；未执行
  为 `NULL`，执行无问题为 0，不支持类型只计数；
- 冻结 `Script_DAISY_Module_DBS_11_Full_Scan.py`、schema 4 状态实现、Reader 与 Diff 实现
  相对 v1.6.0 无差异；schema 3 DDL 和读取边界由合同哈希测试锁定；
- v1.6.1 仅让统一 schema 4 扫描冻结 `metadata_exiftool` 与 `metadata_ffprobe` 两个配置键，
  旧 partial 缺键按 `true` 解释；没有新增数据库表、列、索引或约束。

## 五、验收中发现并修复的问题

1. RAW-only 核验原先错误复用了格式抽样结果，可能选中 0 个 RAW；现改为从全部快照条目
   独立选择 RAW 候选，格式抽样不再限制 RAW；
2. ENV 和五工具按钮使用固定字符宽度时会在高缩放下越界；现使用响应式等宽列、短状态标签
   和完整悬停说明；
3. 同排按钮把间距放在按钮列内部，导致最后一项比其它项宽 8 px；现改为独立 spacer 列；
4. 硬盘登记默认页高出 1080P 可视区约 20 px；现把检测按钮与精简说明合并到同一行；
5. 多目录标题与“添加目录”存在 3.5 px 纵向偏差；现调整变量高度行的标签位置并实测同轴；
6. 1～2 px 的 Tk 取整误差会造成 `yview` 尾值略小于 1；滚动判定现以可见行为和 2 px 容差
   验收，容差内不显示滚动条且滚轮不会移动顶部；
7. 旧测试仍引用扫描内格式／RAW、核验哈希范围、顶部哈希抽样和空闲关闭确认；已按最终
   产品边界改写，避免以陈旧 UI 契约掩盖真实失败；
8. 定稿后发现仓库仍保留一个已无字段引用的范围滑块控件；已删除控件、样式与分支，随后
   GUI／真实 Tk／核心单元 273／273 通过，并在最终 633／633 全量回归中再次验证。

## 六、静态与发布前审计

- 67 个 `Script` Python 文件以 Python 3.14 编译通过；
- `git diff --check` 通过；本轮 20 个代码／测试／文档文件均为 UTF-8 无 BOM、LF；
- 5 个现行说明文档的标准 Markdown 本地链接全部可解析，且没有 Wiki 双方括号链接；
- 冻结状态、Reader、Diff 和旧 Full 扫描实现相对 `v1.6.0` 零差异，本地不存在
  `v1.6.1` 标签；
- Git 只暂存明确列出的代码、测试和文档，不使用 `git add .`，不暂存 `TEMP/`；
- 最终提交位于 `Codex` 分支，提交哈希以 Git 记录为准；推送后不快进 `main`，不建立 tag。

## 七、结论与限制

仓库自动化足以证明当前代码和声明的兼容边界一致，但不能证明 v1.6.1 已达到稳定发布质量。
本轮没有对用户真实 RAW、异常媒体、海量档案、真实硬盘或全部外部工具版本组合做破坏性／
长时间验收，也没有复现用户此前遇到的 Windows 原生 ffmpeg 内存访问弹窗。因此：

- v1.6.1 记录为“初步修改版”；
- `v1.6.0` 继续是最近稳定 tag；
- 本轮不创建 `v1.6.1` tag；
- 后续真实使用发现的问题继续在 `Codex` 分支修复，达到稳定门槛后再单独决定是否发布 tag。
